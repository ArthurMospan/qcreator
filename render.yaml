services:
  - type: web
    name: qcreator
    runtime: node
    plan: free
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    autoDeploy: true
    envVars:
      - key: QC_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
